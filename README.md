# -AWS-Serverless-Fraud-Detection-System
This project demonstrates a serverless event-driven fraud detection system built on AWS.  The system processes tax return submissions, stores audit records, detects suspicious activity in real time, and distributes fraud alerts using AWS managed services.
